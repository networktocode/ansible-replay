============
Installation
============

At the command line::

    $ pip install Ansible-Replay

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Ansible-Replay
    $ pip install Ansible-Replay
